# Supabase (required)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Optional: AI provider for Phase 1 chat (proxied via Edge Function later)
# Never put secret keys here — only public/publishable values
VITE_APP_NAME=EDUBRAIN AI
